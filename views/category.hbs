<!DOCTYPE html>
<html lang="en">

<head>
    {{> head}}
</head>

<body>
    {{> menu}}
    <div class="container grid-xl">
        <h1>{{ name }}</h1>
        <div class="columns">
            {{#each servicios as |servicio|}}
                <div class="col-md-12 col-4">
                    <div class="card">
                        <div class="card-image">
                            <a href="/review/{{ servicio.name }}">
                                <img src="{{ servicio.img }}" alt="Picture of {{ servicio.name }}"
                                    class="img-responsive">
                            </a>
                        </div>
                        <div class="card-header">
                            <div class="card-title-h5 columns">
                                <div class="column col-6">
                                    <a href="/review/{{ servicio.name }}" class="link-jet">{{ servicio.name }}</a>
                                </div>
                                <div class="column col-6 text-right stars">{{ numToStars servicio.rating }}</div>
                            </div>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
    </div>
</body>

</html>